name: registration
ingress:
  hosts:
    - host: registration.ecommerce-store-builder.dev
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: registration-tls
      hosts:
        - registration.ecommerce-store-builder.dev