name: backend_registration_config

on:
  push:
    branches:
      - main
    paths:
      - deploy/config/environments/common.yaml
      - deploy/config/environments/development.yaml
      - deploy/config/backends/common.yaml
      - deploy/config/backends/registration/common.yaml
      - deploy/config/backends/registration/development.yaml

env:
  COMPONENT_NAME: registration
  COMPONENT_TYPE: backend

jobs:

  deploy:  
    environment: development
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - uses: ./.github/workflows/composite/deploy
        with:
          environment: development
          component_name: ${{ env.COMPONENT_NAME }}
          component_type: ${{ env.COMPONENT_TYPE }}
          digitalocean_access_token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}